{
  "name": "samwill-online-order",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix web install && npm --prefix web run build && npm --prefix server install",
    "start": "npm --prefix server start"
  }
}
